public class Test7 {
    public static void main(String[] args){
        /*
            动态初始化和静态初始化
            动态：手动指定数组长度，由系统给出默认初始化
                (只明确元素个数，不确定具体数值，推荐使用动态初始化)
                int [] arr = { ? ? ? ? };
                int [] arr = new int[5];
            静态：手动指定数组元素，系统会根据元素个数，计算出数组的长度
                (需求中已经明确要求了要操作的具体数据，直接静态初始化即可)
                int [] arr = {11，12，13};
         */
    }
}
